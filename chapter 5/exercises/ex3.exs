defmodule FibonacciNumber do

  def recursive_sequence(num) do
  end

  def recursive_sequence(num) do
  end
  
end

metal_pieces = Enum.take(Stream.cycle(["-"]), 1000)
ScrewsFactory.run(metal_pieces)
